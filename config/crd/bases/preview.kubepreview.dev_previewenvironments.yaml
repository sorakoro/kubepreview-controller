---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: previewenvironments.preview.kubepreview.dev
spec:
  group: preview.kubepreview.dev
  names:
    kind: PreviewEnvironment
    listKind: PreviewEnvironmentList
    plural: previewenvironments
    singular: previewenvironment
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PreviewEnvironment is the Schema for the previewenvironments
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of PreviewEnvironment
            properties:
              deployments:
                description: deployments is the list of source Deployments to duplicate.
                items:
                  description: DeploymentRef references a source Deployment to duplicate.
                  properties:
                    name:
                      description: name is the name of the source Deployment.
                      type: string
                  required:
                  - name
                  type: object
                type: array
              identifier:
                description: identifier is the suffix appended to duplicated resource
                  names (e.g., "pr-10237").
                type: string
              image:
                description: image is the container image to use for all duplicated
                  Deployments.
                type: string
              replicas:
                default: 1
                description: replicas is the number of replicas for each duplicated
                  Deployment. Defaults to 1.
                format: int32
                type: integer
              routing:
                description: routing configures VirtualService-based header routing
                  for the preview environment.
                properties:
                  gateways:
                    description: gateways is the list of Istio gateways to attach
                      to (e.g., ["example-gateway"]).
                    items:
                      type: string
                    type: array
                  headerName:
                    default: x-preview-env
                    description: headerName is the HTTP header name used for routing
                      (e.g., "x-preview-env").
                    type: string
                  hosts:
                    description: hosts is the list of hosts for the VirtualService
                      (e.g., ["api-staging.example.com"]).
                    items:
                      type: string
                    type: array
                  namespace:
                    description: |-
                      namespace is the namespace where the VirtualService will be created.
                      If not set, uses the same namespace as the PreviewEnvironment.
                    type: string
                  port:
                    default: 80
                    description: port is the Service port to route to.
                    format: int32
                    type: integer
                  serviceName:
                    description: serviceName is the source Service name to route to
                      the preview copy.
                    type: string
                required:
                - gateways
                - hosts
                - serviceName
                type: object
              services:
                description: services is the list of source Services to duplicate.
                items:
                  description: ServiceRef references a source Service to duplicate.
                  properties:
                    name:
                      description: name is the name of the source Service.
                      type: string
                  required:
                  - name
                  type: object
                type: array
              ttl:
                description: ttl is the duration after which the PreviewEnvironment
                  is automatically deleted (e.g., "72h").
                type: string
            required:
            - deployments
            - identifier
            - image
            type: object
          status:
            description: status defines the observed state of PreviewEnvironment
            properties:
              conditions:
                description: conditions represent the current state of the PreviewEnvironment
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              deployments:
                description: deployments is the status of each duplicated Deployment.
                items:
                  description: DeploymentStatus represents the status of a duplicated
                    Deployment.
                  properties:
                    name:
                      description: name is the name of the duplicated Deployment.
                      type: string
                    ready:
                      description: ready indicates whether the Deployment has available
                        replicas.
                      type: boolean
                  required:
                  - name
                  - ready
                  type: object
                type: array
              expiresAt:
                description: expiresAt is the time when the PreviewEnvironment will
                  be automatically deleted.
                format: date-time
                type: string
              phase:
                description: phase represents the current lifecycle phase.
                enum:
                - Pending
                - Provisioning
                - Ready
                - Error
                type: string
              services:
                description: services is the status of each duplicated Service.
                items:
                  description: ServiceStatus represents the status of a duplicated
                    Service.
                  properties:
                    name:
                      description: name is the name of the duplicated Service.
                      type: string
                  required:
                  - name
                  type: object
                type: array
              virtualService:
                description: virtualService is the name of the created VirtualService.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
